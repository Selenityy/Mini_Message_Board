extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    each message in messages
      li= message.user
      li= message.text
      li= message.added
  a(href="/new")
    button(type="button") Send a Message 